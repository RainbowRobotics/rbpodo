# RBPodo Python Bindings

We provide Python bindings for ``rppodo``.

## Installation

```bash
pip install rbpodo
```

### Build and Install From Source

In project root directory, execute the following command:
```bash
pip install .
```